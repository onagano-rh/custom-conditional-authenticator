apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
spec:
  instances: 1
  db:
    vendor: postgres
    host: kcpostgres
    usernameSecret:
      key: database-user
      name: kcpostgres
    passwordSecret:
      key: database-password
      name: kcpostgres
    database: kcdatabase
  http:
    httpEnabled: true
  proxy:
    headers: xforwarded
  hostname:
    hostname: keycloak-$(oc project -q).${CLUSTER_NAME}
  additionalOptions:
    - name: health-enabled
      value: 'true'
    - name: metrics-enabled
      value: 'true'
    - name: cache-metrics-histograms-enabled
      value: 'true'
    - name: http-metrics-histograms-enabled
      value: 'true'
    - name: event-metrics-user-enabled
      value: 'true'
    - name: event-metrics-user-events
      value: 'login,logout'
  resources:
    requests:
      cpu: 2
      memory: 1Gi
    limits:
      cpu: 2
      memory: 1Gi
  image: default-route-openshift-image-registry.${CLUSTER_NAME}/$(oc project -q)/myrhbk:latest

